{
    "username": "weilifan-t",
    "submission": "class Solution:\n    def minimumCost(self, m: int, n: int, horizontalCut: List[int], verticalCut: List[int]) -> int:\n        horizontalCut.sort()\n        verticalCut.sort()\n\n        horizontalCut = [(-cost, 'h') for cost in horizontalCut]\n        verticalCut = [(-cost, 'v') for cost in verticalCut]\n        allCuts = horizontalCut + verticalCut\n        heapq.heapify(allCuts)\n\n        h_segments = 1 \n        v_segments = 1 \n        total_cost = 0\n\n        while allCuts:\n            cost, cut_type = heapq.heappop(allCuts)\n            if cut_type == 'h':\n                total_cost += (-cost) * v_segments\n                h_segments += 1\n            else:\n                total_cost += (-cost) * h_segments\n                v_segments += 1\n\n        return total_cost",
    "submit_ts": "1720925776",
    "subm_id": "546441883"
}