{
    "username": "vskesha",
    "submission": "class Solution:\n    def maxOperations(self, s: str) -> int:\n        groups, curr = [], 0\n        for k, gr in groupby(s):\n            if k == \"1\":\n                curr = len(list(gr))\n            elif curr:\n                groups.append(curr)\n        return sum(i * n for i, n in enumerate(reversed(groups), 1))",
    "submit_ts": "1721530886",
    "subm_id": "1327946434"
}