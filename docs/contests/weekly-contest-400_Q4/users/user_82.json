{
    "username": "gan14008",
    "submission": "class Solution:\n    def minimumDifference(self, nums: List[int], k: int) -> int:\n        res = abs(nums[0] - k)\n        q = {nums[0]}\n        \n        for num in nums:\n            q = {x & num for x in q} | {num}\n            res = min(res, min(abs(x - k) for x in q))\n        \n        return res",
    "submit_ts": "1717297394",
    "subm_id": "1274740919"
}