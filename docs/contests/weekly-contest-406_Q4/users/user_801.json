{
    "username": "lthroy",
    "submission": "class Solution:\n    def minimumCost(self, m: int, n: int, horizontalCut: List[int], verticalCut: List[int]) -> int:\n        seen = [set() for _ in range(2)]\n        cuts = []\n        for i,cost in enumerate(horizontalCut):\n            cuts.append((cost,i,0))\n        for i,cost in enumerate(verticalCut):\n            cuts.append((cost,i,1))\n        cuts.sort(key=lambda x:-x[0])\n        res = 0\n        for cost,i,t in cuts:\n            res += cost*(1+len(seen[1-t]))\n            seen[t].add(i)\n        return res",
    "submit_ts": "1720925833",
    "subm_id": "1320267646"
}