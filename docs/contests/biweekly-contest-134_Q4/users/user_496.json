{
    "username": "kaluginpeter",
    "submission": "from collections import defaultdict\nclass Solution:\n    def countSubarrays(self, nums: List[int], k: int) -> int:\n        n: int = len(nums)\n        count: int = 0\n        current_subs: dict[int, int] = defaultdict(int)\n        for num in nums:\n            new_subs = defaultdict(int)\n            for sub in current_subs:\n                new_sub = sub & num\n                new_subs[new_sub] += current_subs[sub]\n            new_subs[num] += 1\n            current_subs = new_subs\n            if k in current_subs: count += current_subs[k]\n        return count",
    "submit_ts": "1720279341",
    "subm_id": "1311747335"
}