{
    "username": "SkookumChoocher",
    "submission": "class Solution:\n    def maxOperations(self, s: str) -> int:\n        stack = 0\n        total = 0\n        for char, group in groupby(s):\n            if char == '1':\n                stack += len(''.join(group))\n            else:\n                total += stack\n\n        return total\n",
    "submit_ts": "1721531486",
    "subm_id": "1327962158"
}