{
    "username": "saogetxdy",
    "submission": "class Solution {\npublic:\n    long long minimumOperations(vector<int>& nums, vector<int>& target) {\n        int n = nums.size();\n        long long ans =LLONG_MAX ;\n        vector<int> diff(n,0);\n        vector<long long> dp(n, LLONG_MAX);\n\n        for(int i=0;i<n;++i){\n          diff[i] = target[i] - nums[i];\n          cout<<diff[i]<<\" \";\n        }\n\n        dp[0] = abs(diff[0]);\n\n        for(int i=1;i<n;++i){\n          dp[i] = min(dp[i-1]+abs(diff[i]), dp[i-1] + abs(diff[i]-diff[i-1]));\n          if((diff[i]<diff[i-1]) && (diff[i]>=0 )&&( diff[i-1]>=0)){\n            dp[i] = min(dp[i], dp[i-1]);\n          }\n          if(diff[i]>diff[i-1] &&diff[i]<=0 && diff[i-1]<=0){\n            dp[i] = min(dp[i], dp[i-1]);\n          }\n        }\n\n\n        return dp[n-1];\n    }\n};",
    "submit_ts": "1721532085",
    "subm_id": "1327977266"
}