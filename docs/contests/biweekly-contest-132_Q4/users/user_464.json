{
    "username": "Murad134",
    "submission": "class Solution\n{\npublic:\n#define ll long long\n#define all(arr) arr.begin(), arr.end()\n#define srt(v) sort(v.begin(), v.end())\n#define pb push_back\n#define map_c map<char, ll>\n#define ump unordered_map<int, int>\n#define vl vector<int>\n#define que queue<int>\n#define s second\n#define f first\n#define ers erase\n#define flI(i, a, n) for (int i = a; i < n; i++)\n#define fld(i, a, n) for (ll i = a; i <= n; i++)\n#define flrd(i, a, n) for (ll i = a; i >= n; i--)\n#define flrc(i, a, x) for (char c = a; c < x; c++)\n#define fl2(i, a, n) for (ll i = a; i <= n; i += 2)\n    // write code\n    int maximumLength(vector<int> &arr, int value)\n    {\n        int sz = arr.size();\n        vector<vector<int>> nums(sz, vl(value + 1, 1));\n        int sol = 1;\n        vl vec(value + 1, -1);\n        vector<map<int, int>> arr2(value + 1);\n        flI(k,0,sz)\n        {\n            fld(j,0,value)\n            {\n                flI(l,0,j)\n                {\n                    int f1= max(nums[k][j], 1 + vec[l]);;\n                    nums[k][j] =f1;\n                }\n                if (arr2[j].find(arr[k]) != arr2[j].end())\n                {\n                    int f2=max(nums[k][j], 1 + arr2[j][arr[k]]);\n                    nums[k][j] = f2;\n                }\n                int f3=max(sol, nums[k][j]);;\n                sol = f3;\n            }\n            fld(j,0,value)\n            {\n                int f4= max(vec[j], nums[k][j]);\n                vec[j] =f4;\n                int vlu= max(arr2[j][arr[k]], nums[k][j]);;\n                arr2[j][arr[k]] =vlu;\n            }\n        }\n        return sol;\n    }\n};",
    "submit_ts": 1717862036.0
}