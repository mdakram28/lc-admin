{
    "username": "Ujimatsu_Chiya",
    "submission": "from sortedcontainers import SortedList\nfrom collections import defaultdict, deque, Counter\nfrom bisect import bisect_left, bisect_right\nfrom typing import List, Set\n\n\nclass Solution:\n    def minChanges(self, a: List[int], k: int) -> int:\n        m = len(a) // 2\n        l, r = a[:m], list(reversed(a[m:]))\n        s = [0 for _ in range(k + 2)]\n        for x, y in zip(l, r):\n            dis = abs(x - y)\n            mx = max(x, y, k - x, k - y)\n            s[0] += 1\n            s[dis] -= 1\n            s[dis+1] += 1\n            s[mx+1] -= 1\n            s[mx+1] += 2\n            # print(dis, mx)\n        #print(s)\n        for i in range(1, k+2):\n            s[i] += s[i-1]\n        #print(s)\n        return min(s)",
    "submit_ts": "1721487134",
    "subm_id": "548308557"
}