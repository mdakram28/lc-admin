{
    "username": "cheng-liang-yu",
    "submission": "class Solution:\n    def maxOperations(self, s: str) -> int:\n        a = [len([*g]) for k, g in groupby(s) if k == '1']\n        if s[-1] == '1': a.pop()\n        res = c = 0\n        for x in a:\n            c += x\n            res += c\n        return res",
    "submit_ts": "1721529723",
    "subm_id": "548372797"
}