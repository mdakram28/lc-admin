{
    "username": "DylanSmith",
    "submission": "typedef long long ll;\n\n#define pb push_back\n#define sz(x) (int)x.size()\n#define all(x) begin(x),end(x)\n#define lb(x,y) lower_bound(all(x),y)-begin(x)\nclass Solution {\npublic:\n    vector<int> shortestDistanceAfterQueries(int n, vector<vector<int>>& queries) {\n        set<int> st;\n        for (int i = 0; i < n - 1; i++) st.insert(i);\n        int cur = n - 1;\n        vector<int> res;\n        for (auto &p : queries) {\n            int u = p[0], v = p[1];\n            while (st.lower_bound(u + 1) != st.end()) {\n                int k = *st.lower_bound(u + 1);\n                if (k >= v) break;\n                st.erase(k);\n                cur--;\n            }\n            res.pb(cur);\n        }\n        return res;\n    }\n};",
    "submit_ts": "1722739470",
    "subm_id": "1343695270"
}