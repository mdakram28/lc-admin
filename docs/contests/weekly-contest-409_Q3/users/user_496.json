{
    "username": "shinever",
    "submission": "from sortedcontainers import SortedSet\n\nclass Solution:\n    def shortestDistanceAfterQueries(self, n: int, queries: List[List[int]]) -> List[int]:\n        points = SortedSet([i for i in range(1, n)])\n        ans = []\n        for a, b in queries:\n            while 1:\n                i = points.bisect_right(a)\n                if i < len(points) and points[i] < b:\n                    points.remove(points[i])\n                else:\n                    break\n            ans.append(len(points))\n        return ans",
    "submit_ts": "1722742017",
    "subm_id": "1343758963"
}