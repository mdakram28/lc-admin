{
    "username": "hash_osjobs",
    "submission": "from sortedcontainers import SortedList\n    \nclass Solution:\n    def shortestDistanceAfterQueries(self, n: int, queries: List[List[int]]) -> List[int]:\n        arr = SortedList(list(range(n - 1)))\n        \n        result = []\n        for i, j in queries:\n            idx1 = arr.bisect_left(i + 1)\n            idx2 = arr.bisect_right(j - 1) - 1\n            # print(arr, idx1, idx2)\n            \n            for idx in reversed(range(idx1, idx2 + 1)):\n                arr.pop(idx)\n            result.append(len(arr))\n        return result\n            ",
    "submit_ts": "1722743788",
    "subm_id": "552352812"
}