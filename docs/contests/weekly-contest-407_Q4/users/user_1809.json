{
    "username": "Skyee_03",
    "submission": "class Solution {\npublic:\n    #define ll long long int\n    long long minimumOperations(vector<int>& nums, vector<int>& tar) {\n        ll n=nums.size();\n        vector<int>v(n);\n        ll i=0, cnt=0, dif=0, c=0;\n        while(i<n){\n            v[i]=tar[i]-nums[i];\n            i++;\n        }\n        i=0;\n        while(i<n){\n            // if(nums[i]==tar[i]){dif=0;}\n            // else{\n                if(i==0 or (v[i-1]>0 and v[i]>0) or (v[i-1]<0 and v[i]<0)){\n                    c += abs((tar[i]-nums[i]));\n                    \n                    if(abs(v[i])-abs(dif)<=0) {}\n                    else cnt+=(abs(v[i])-abs(dif));\n                }\n                else{\n                    cnt+= abs(v[i]);\n                }\n            dif=v[i];\n                // else{\n                    c += abs((tar[i]-nums[i])-dif);\n                // }\n                // dif=(tar[i]-nums[i]);\n            // }\n            \n            // cout<<(tar[i]-nums[i])<<\" \"<<dif<<\"---\"<<cnt<<endl;;\n            \n            i++;\n        }\n        // cout<<endl;\n        return cnt;\n    }\n};\n\n\n\n\n",
    "submit_ts": "1721533633",
    "subm_id": "1328013262"
}