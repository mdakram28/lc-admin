{
    "username": "rohit_21_",
    "submission": "class Solution {\npublic:\n    long long minimumOperations(vector<int>& nums, vector<int>& target) {\n      int n = nums.size();\n    vector<int> diff(n);\n    for (int i = 0; i < n; i++) {\n        diff[i] = target[i] - nums[i];\n        cout<<diff[i]<<\" \";\n    }\n    cout<<endl;\n    long long ans = 0;\n    int add = 0;\n    for (int i = 0; i < n; i++) {\n        int prev=add;\n        if (diff[i] > 0) {\n            add=max(add,diff[i]);\n            if(prev>diff[i]){\n                 add = diff[i];\n            ans += prev - add;\n            }\n        } else {\n            ans += add;\n            add = 0;\n            cout<<ans<<\" \";\n        }\n    }\n    ans += add;\n    add = 0;\n    for (int i = 0; i < n; i++) {\n        int prev=add;\n        if (diff[i] < 0) {\n            add=max(add,abs(diff[i]));\n            if(prev>abs(diff[i])){\n                 add = abs(diff[i]);\n            ans += prev -add;\n           \n            cout<<ans;\n            }\n\n        } else {\n            ans += add;\n            add = 0;\n            cout<<ans<<\" \";\n        }\n    }\n    ans += add;\n\n    return ans;\n    }\n};",
    "submit_ts": "1721532608",
    "subm_id": "1327989905"
}