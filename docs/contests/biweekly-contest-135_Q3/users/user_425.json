{
    "username": "girishbasandani1",
    "submission": "class Solution {\npublic:\n    int minChanges(vector<int>& nums, int k) {\n        map<int,int>mp;\n        // for(int i = 0; i<nums.size()/2; i++){\n        //     mp[abs(nums[i]-nums[nums.size()-1-i])]++;\n        // }\n        // int dif = (*mp.begin()).first, diffx = (*mp.begin()).second;\n        // for(auto it:mp){\n        //     if(it.second>diffx){\n        //         diffx = it.second;\n        //         dif = it.first;\n        //     }else if(it.second==diffx){\n        //         dif = min(dif,it.first);\n        //     }\n        // }\n        // int ans = 0;\n        // for(int i = 0; i<nums.size()/2; i++){\n        //     int a = nums[i], b = nums[nums.size()-1-i];\n        //     int c = max(a,b);\n        //     a = min(a,b);\n        //     b = c;\n        //     if(b-a==dif)continue;\n        //     if(a+dif<=k || b-dif>=0)ans++;\n        //     else ans+=2;\n        // }\n        // return ans;\n        vector<int>pre(k+3, 0);\n        for(int i = 0; i<nums.size()/2; i++){\n            mp[abs(nums[i]-nums[nums.size()-1-i])]++;\n            int a = min(nums[i],nums[nums.size()-1-i]);\n            int b = max(nums[i],nums[nums.size()-1-i]);\n            pre[max(b,k-a)+1]++;\n        }\n        for(int i = 1; i<k+3; i++)pre[i]+=pre[i-1];\n        int ans = nums.size();\n\n        for(auto it:mp){\n            int cnt = 0;\n            cnt+=2*(pre[it.first]);\n            cnt+=((nums.size()/2)-it.second-pre[it.first]);\n            ans = min(ans,cnt);\n        }   \n        return ans;\n\n\n    }\n};",
    "submit_ts": "1721488086",
    "subm_id": "1327411076"
}