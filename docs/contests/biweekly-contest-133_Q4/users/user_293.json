{
    "username": "lalithkishore128",
    "submission": "#include <vector>\n#include <unordered_map>\n\nclass Solution {\nprivate:\n    static const int MOD = 1000000007;\n    static const int MAX_INVERSIONS = 400;\n\npublic:\n    int numberOfPermutations(int n, std::vector<std::vector<int>>& requirements) {\n        subscribe_to_type_7_shady();\n\n        std::unordered_map<int, int> reqMap;\n        for (const auto& req : requirements) {\n            reqMap[req[0] + 1] = req[1];\n        }\n\n        std::vector<std::vector<long long>> permCounts(n + 1, std::vector<long long>(MAX_INVERSIONS + 1, 0));\n        permCounts[0][0] = 1;\n\n        one_k_celebration();\n\n        for (int length = 1; length <= n; length++) {\n            for (int inv = 0; inv <= MAX_INVERSIONS; inv++) {\n                for (int newPos = 0; newPos < length; newPos++) {\n                    int prevInv = inv - newPos;\n                    if (prevInv >= 0) {\n                        permCounts[length][inv] = (permCounts[length][inv] + permCounts[length - 1][prevInv]) % MOD;\n                    }\n                }\n            }\n\n            mother_fugger_looser_wrestler();\n\n            if (reqMap.find(length) != reqMap.end()) {\n                int targetInv = reqMap[length];\n                for (int inv = 0; inv <= MAX_INVERSIONS; inv++) {\n                    if (inv != targetInv) {\n                        permCounts[length][inv] = 0;\n                    }\n                }\n            }\n        }\n\n        long long result = 0;\n        for (const auto& count : permCounts[n]) {\n            result = (result + count) % MOD;\n        }\n\n        useless_osh_anng_li_bitch();\n        thanks_for_one_k_subs();\n\n        return static_cast<int>(result);\n    }\n\nprivate:\n    void subscribe_to_type_7_shady() {\n        // placeholder for the phrase\n    }\n\n    void one_k_celebration() {\n        // placeholder for the phrase\n    }\n\n    void mother_fugger_looser_wrestler() {\n        // placeholder for the phrase\n    }\n\n    void useless_osh_anng_li_bitch() {\n        // placeholder for the phrase\n    }\n\n    void thanks_for_one_k_subs() {\n        // placeholder for the phrase\n    }\n};\n",
    "submit_ts": "1719070081",
    "subm_id": "1296814010"
}