{
    "username": "TriangleLeetCode",
    "submission": "from sortedcontainers import SortedList, SortedSet, SortedDict \n\nclass Solution:\n    def shortestDistanceAfterQueries(self, n: int, queries: List[List[int]]) -> List[int]:\n        used = SortedList([i for i in range(n)])\n        ans = []\n        for query in queries:\n            #print(used)\n            #if query[0]+1 in used:\n            index1 = bisect.bisect_left(used, query[0]+1)\n            index2 = bisect.bisect_right(used, query[1]-1)\n            #print(index1, index2)\n            for i in range(index2-1, index1-1, -1):\n                if used[i]!=query[0] and used[i]!=query[1]:\n                    used.discard(used[i])\n            ans.append(len(used)-1)\n        return ans",
    "submit_ts": "1722742525",
    "subm_id": "1343770986"
}