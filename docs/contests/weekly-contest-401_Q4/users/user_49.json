{
    "username": "mkawa222",
    "submission": "class Solution:\n    def maxTotalReward(self, rewardValues: List[int]) -> int:\n        now=1\n        ans=0\n        for a in sorted(rewardValues):\n            mask=(1<<a)-1\n            now|=(now&mask)<<a\n        return now.bit_length()-1\n",
    "submit_ts": 1717901748.0
}