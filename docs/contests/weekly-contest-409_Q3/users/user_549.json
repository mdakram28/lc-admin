{
    "username": "testimony2014",
    "submission": "class Solution {\npublic:\n    vector<int> shortestDistanceAfterQueries(int n, vector<vector<int>>& queries) {\n        set<int> nums;\n        for (int i = 0; i < n; ++i) {\n            nums.insert(i);\n        }\n        \n        vector<int> res(queries.size());\n        for (int i = 0; i < queries.size(); ++i) {\n            int f = queries[i][0], s = queries[i][1];\n            nums.erase(nums.upper_bound(f), nums.lower_bound(s));\n            res[i] = nums.size() - 1;\n        }\n        return res;\n    }\n};",
    "submit_ts": "1722741623",
    "subm_id": "1343749434"
}