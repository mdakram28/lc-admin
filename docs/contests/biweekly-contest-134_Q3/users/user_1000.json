{
    "username": "suizhiyuan",
    "submission": "impl Solution {\npub fn number_of_alternating_groups(colors: Vec<i32>, k: i32) -> i32 {\n    let mut result = 0;\n    let mut swap_len = 0;\n    let mut prev = colors[0];\n\n    let len = colors.len() + k as usize -1;\n    \n    for &v in colors.iter().cycle().take(len){\n        if prev == v {\n            if swap_len >= k {\n                result += swap_len - k + 1;\n            }\n\n            swap_len = 1;\n            prev = v;\n            continue;\n        }\n\n        swap_len += 1;\n        prev = v;\n    }\n    if swap_len >= k {\n        result += swap_len - k + 1;\n    }\n    result\n}\n}",
    "submit_ts": "1720277968",
    "subm_id": "544559907"
}