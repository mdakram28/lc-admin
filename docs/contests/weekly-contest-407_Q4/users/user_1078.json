{
    "username": "abhishek2207",
    "submission": "class Solution {\n    public long minimumOperations(int[] nums, int[] target) {\n        long [] diff = new long[nums.length];\n        for(int i = 0; i<nums.length; i++){\n            diff[i] = target[i] - nums[i];\n        }\n        // long positiveSum = 0;\n        // long curr = 0;\n        // for(int i = 0; i<diff.length; i++){\n        //     if(diff[i] <= 0){\n        //         positiveSum+=curr;\n        //         curr  = 0;\n        //         continue;\n        //     }\n        //     curr = Math.max(curr, diff[i]);\n        // }\n        // positiveSum+=curr;\n        // long negativeSum = 0;\n        // curr = 0;\n        // for(int i =0; i<diff.length; i++){\n        //     if(diff[i]>=0){\n        //         negativeSum += Math.abs(curr);\n        //         curr = 0;\n        //         continue;\n        //     }\n        //     curr = Math.min(curr, diff[i]);\n        // }\n        // negativeSum += Math.abs(curr);\n        // return positiveSum+negativeSum;\n        long positiveSum = 0;\n        long op = 0;\n        for(int i = 0; i<diff.length; i++){\n            if(diff[i]<=0){\n                op = 0;\n                continue;\n            }\n            if(op > diff[i]){\n                op = diff[i];\n            }\n            else{\n                positiveSum += diff[i]-op;\n                op = diff[i];\n            }\n        }\n        for(int i = 0; i<diff.length; i++)\n                diff[i] *=-1;\n        long negativeSum = 0;\n        op = 0;\n        for(int i = 0; i<diff.length; i++){\n            if(diff[i]<=0){\n                op = 0;\n                continue;\n            }\n            if(op > diff[i]){\n                op = diff[i];\n            }\n            else{\n                negativeSum += diff[i]-op;\n                op = diff[i];\n            }\n        }\n        return positiveSum + negativeSum;\n        \n     }\n}",
    "submit_ts": "1721532061",
    "subm_id": "1327976638"
}