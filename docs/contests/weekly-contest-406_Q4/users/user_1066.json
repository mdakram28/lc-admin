{
    "username": "whiff",
    "submission": "class Solution {\npublic:\n    long long minimumCost(int m, int n, vector<int>& hc, vector<int>& vc) {\n        sort(hc.begin(), hc.end(), greater<int>());\n        sort(vc.begin(), vc.end(), greater<int>());\n        long long int tc = 0, hs = 1, vs = 1;\n        long long int i = 0, j = 0;\n        while (i < hc.size() || j < vc.size()) {\n            if (j >= vc.size() || (i < hc.size() && hc[i] >= vc[j])) {\n                tc += hc[i] * vs;\n                hs++;\n                i++;\n            } else {\n                tc += vc[j] * hs;\n                vs++;\n                j++;\n            }\n        }\n        return tc;\n    }\n};",
    "submit_ts": "1720925440",
    "subm_id": "1320254811"
}