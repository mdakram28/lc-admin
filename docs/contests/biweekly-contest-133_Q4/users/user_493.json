{
    "username": "sweety_singh12345",
    "submission": "class Solution {\n    private static final int MOD = 1_000_000_007;\n    private static final int MAX_INVERSIONS = 400;\n\n    public int numberOfPermutations(int n, int[][] requirements) {\n        Map<Integer, Integer> reqMap = new HashMap<>();\n        for (int[] req : requirements) {\n            reqMap.put(req[0] + 1, req[1]);\n        }\n\n        long[] permCounts = new long[MAX_INVERSIONS + 1];\n        permCounts[0] = 1;\n\n        for (int length = 1; length <= n; length++) {\n            long[] newPermCounts = new long[MAX_INVERSIONS + 1];\n            for (int inv = 0; inv <= MAX_INVERSIONS; inv++) {\n                for (int newPos = 0; newPos < length; newPos++) {\n                    int prevInv = inv - newPos;\n                    if (prevInv >= 0) {\n                        newPermCounts[inv] = (newPermCounts[inv] + permCounts[prevInv]) % MOD;\n                    }\n                }\n            }\n\n            if (reqMap.containsKey(length)) {\n                int targetInv = reqMap.get(length);\n                for (int inv = 0; inv <= MAX_INVERSIONS; inv++) {\n                    if (inv!= targetInv) {\n                        newPermCounts[inv] = 0;\n                    }\n                }\n            }\n\n            permCounts = newPermCounts;\n        }\n\n        long result = 0;\n        for (long count : permCounts) {\n            result = (result + count) % MOD;\n        }\n\n        return (int) result;\n    }\n}",
    "submit_ts": 1719070260.0
}