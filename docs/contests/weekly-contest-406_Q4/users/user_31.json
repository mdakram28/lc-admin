{
    "username": "Roumak2008",
    "submission": "class Solution:\n    def minimumCost(self, m: int, n: int, horizontalCut: List[int], verticalCut: List[int]) -> int:\n        cuts = [(cost, 'H') for cost in horizontalCut] + [(cost, 'V') for cost in verticalCut]\n    \n        # Sort cuts by cost\n        cuts.sort(reverse=True)\n\n        # Initialize counters for the number of horizontal and vertical pieces\n        horizontal_pieces, vertical_pieces = 1, 1\n\n        # Initialize total cost\n        total_cost = 0\n\n        # Iterate through the sorted cuts\n        for cost, cut_type in cuts:\n            if cut_type == 'H':\n                # If it's a horizontal cut, it will affect the current vertical pieces\n                total_cost += cost * vertical_pieces\n                horizontal_pieces += 1\n            else:\n                # If it's a vertical cut, it will affect the current horizontal pieces\n                total_cost += cost * horizontal_pieces\n                vertical_pieces += 1\n\n        return total_cost",
    "submit_ts": "1720924735",
    "subm_id": "1320228856"
}