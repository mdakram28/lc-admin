{
    "username": "enthusiasm-k",
    "submission": "class Solution {\npublic:\n    long long minimumOperations(vector<int>& nums, vector<int>& target) {\n        vector<int> arr(nums.size(),0);\n        vector<int> lis(nums.size(),0);\n        for(int i = 0; i < nums.size(); ++i){\n            arr[i] = target[i] - nums[i];\n        }\n\n        for(int i = arr.size()-1; i > 0; --i) lis[i] = arr[i] - arr[i-1];\n        lis[0] = arr[0];\n        long long up = 0, down = 0;\n        for(int i = 0; i < arr.size(); ++i){\n            if(lis[i] > 0) up += lis[i];\n            else down -= lis[i];\n        }\n    \n        return max(up,down);\n    }\n};",
    "submit_ts": "1721530204",
    "subm_id": "548376785"
}