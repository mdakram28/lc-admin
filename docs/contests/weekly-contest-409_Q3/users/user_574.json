{
    "username": "illusion7yk",
    "submission": "from sortedcontainers import SortedList\n\nclass Solution:\n    def shortestDistanceAfterQueries(self, n: int, queries: List[List[int]]) -> List[int]:\n        ans = []\n        sl = SortedList(list(range(n)))\n        for x, y in queries:\n            l, r = sl.bisect_left(x + 1), sl.bisect_right(y - 1)\n            del sl[l : r]\n            ans.append(len(sl) - 1)\n        return ans",
    "submit_ts": "1722740800",
    "subm_id": "1343729018"
}