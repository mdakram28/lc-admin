{
    "username": "ape_pack",
    "submission": "class Solution {\npublic:\n    vector<int> shortestDistanceAfterQueries(int n, vector<vector<int>>& queries) {\n        set<int> s; \n        for (int i = 0; i < n; i++) {\n            s.insert(i);\n        }\n        vector<int> ans;\n        for (auto q:queries) {\n            auto l = s.lower_bound(q[0]+1);\n            auto r = s.upper_bound(q[1]-1);\n            s.erase(l,r);\n            ans.push_back((int)(s.size())-1);\n        }\n        return ans;\n        \n    }\n};",
    "submit_ts": "1722741897",
    "subm_id": "552341192"
}