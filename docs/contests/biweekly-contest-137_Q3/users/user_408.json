{
    "username": "Shivam",
    "submission": "#define rep(i,a,b)      for(int i=a;i<b;i++)\n#define repo(i,a,b)      for(int i=a;i>=b;i--)\n#define vi              vector<long long int>\n#define all(x)          begin(x), end(x)\nclass Solution {\npublic:\n    long long maximumValueSum(vector<vector<int>>& board) {\n        int n = board.size(), m=board[0].size();\n        vector<pair<long long int,pair<long long int,long long int>>>arr;\n        rep(i,0,n){\n            rep(j,0,m){\n                arr.push_back({board[i][j],{i,j}});\n            }\n        }\n\n        sort(all(arr),[](pair<long long int,pair<long long int,long long int>>&p, pair<long long int,pair<long long int,long long int>>&q)->bool{\n            return p.first>q.first;\n        });\n        \n        long long int ans = -1e15;\n        rep(i,0,arr.size()-2){\n            rep(j,i+1,arr.size()-1){\n                if(arr[i].second.first == arr[j].second.first || arr[i].second.second == arr[j].second.second){\n\n                }else{\n                    rep(k,j+1,arr.size()){\n                        if(arr[i].second.first == arr[k].second.first || arr[i].second.second == arr[k].second.second || arr[j].second.first == arr[k].second.first || arr[j].second.second == arr[k].second.second ){\n\n                        }else{\n                            ans = max(ans, arr[i].first + arr[j].first + arr[k].first);\n                            break;\n                        }\n                    }\n\n                }\n            }\n        }\n        return ans;\n    }\n};",
    "submit_ts": "1723909430",
    "subm_id": "1359192268"
}