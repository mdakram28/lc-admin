{
    "username": "mkawa222",
    "submission": "class Solution:\n    def minimumOperations(self, nums: List[int], target: List[int]) -> int:\n        dd=[0]+[a-b for a,b in zip(nums,target)]+[0]\n        dd=[d1-d0 for d0,d1 in zip(dd,dd[1:])]\n        return sum(d for d in dd if d>0)\n",
    "submit_ts": "1721530792",
    "subm_id": "1327943972"
}