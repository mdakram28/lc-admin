{
    "username": "tejas_mishr",
    "submission": "class Solution {\npublic:\n    long long minimumOperations(vector<int>& nums, vector<int>& target) {\n        long long mini = 0, ans = 0;\n        int n = nums.size();\n        vector<int> vec(n, 0);\n        \n        \n        for (int i = 0; i < n; i++) vec[i] = target[i] - nums[i];\n\n        int pos = 0;\n        \n        \n        ans = abs(vec[0]);\n        mini = ans;\n        (vec[0] >= 0) ? pos= 1 : pos=0;\n\n        for (int i = 1; i < n; i++) {\n            if ((vec[i] >= 0 && pos == 0) || (vec[i] < 0 && pos == 1)) {\n                mini = abs(vec[i]);\n                ans += abs(vec[i]);\n                (vec[i] >= 0) ? pos= 1 : pos=0;\n                continue;\n            }\n            if (mini >= abs(vec[i])) mini = abs(vec[i]);\n            \n            else {\n                ans += abs(vec[i]) - mini;\n\n                mini = abs(vec[i]);\n            }\n        }\n        \n        return ans;\n    }\n};",
    "submit_ts": "1721532778",
    "subm_id": "1327993866"
}