{
    "username": "abhaylodhiab11",
    "submission": "class Solution {\npublic:\n    long long minimumOperations(vector<int>& nu, vector<int>& tar) {\n        long long res=0;\n        vector<int> diff(nu.size(),0);\n        for(int i=0; i<nu.size();++i){\n            diff[i]=tar[i]-nu[i];\n        }\n        \n        int maxi= abs(diff[0]);\n        vector<pair<int,int>> v;\n        int start= diff[0];\n        \n        for(int i=1;i<nu.size();++i ){\n            if(abs(diff[i])<abs(diff[i-1])|| (diff[i]>=0 && diff[i-1]<0)|| (diff[i-1]>=0 && diff[i]<0)) {\n              v.push_back({start,diff[i-1]});\n              start= diff[i];\n            }\n        }\n        \n        v.push_back({start,diff[nu.size()-1]});\n    \n        for(int i=1; i<v.size();++i){\n            if((v[i].first<0 && v[i-1].second>=0)|| (v[i].first>=0 && v[i-1].second<0)){\n                \n            }else{\n                res-=abs(v[i].first);\n            }\n        }\n        \n        for(auto it:v) res+= abs(it.second);\n\n        \n        return res;\n    }\n};",
    "submit_ts": "1721532872",
    "subm_id": "1327996032"
}