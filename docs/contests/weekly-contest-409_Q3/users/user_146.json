{
    "username": "lee0560",
    "submission": "from sortedcontainers import SortedList\n\nclass Solution:\n    def shortestDistanceAfterQueries(self, n: int, queries: List[List[int]]) -> List[int]:\n        \n        def solve(u, v):\n            pos = sl.bisect_left(v) - 1\n            while pos >= 0 and sl[pos] > u:\n                del sl[pos]\n                pos -= 1    \n            return len(sl)\n        \n        m = len(queries)\n        res = [0] * m\n        sl = SortedList(range(n - 1))\n        for i,(u,v) in enumerate(queries):\n            res[i] = solve(u, v)\n        return res",
    "submit_ts": "1722740020",
    "subm_id": "552328708"
}