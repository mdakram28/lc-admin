{
    "username": "smthng",
    "submission": "class Solution:\n    def minimumOperations(self, nums: List[int], target: List[int]) -> int:\n        diff=[nums[i]-target[i] for i in range(len(nums))]\n        op=0\n        currsum=0\n        for i in range(len(diff)): \n            if i == 0 or (diff[i - 1] > 0 and diff[i] > 0) or (diff[i - 1] < 0 and diff[i] < 0): \n                curr = abs(diff[i]) - abs(currsum) \n                if curr > 0: \n                    op += curr \n                currsum = diff[i] \n            else:   \n                op += abs(diff[i]) \n                currsum = diff[i]         \n        return op",
    "submit_ts": "1721533172",
    "subm_id": "1328002942"
}