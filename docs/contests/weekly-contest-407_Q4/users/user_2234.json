{
    "username": "LQnVRuiNVr",
    "submission": "class Solution:\n    def minimumOperations(self, nums: List[int], target: List[int]) -> int:\n        diff = [j-i for j,i in zip(target, nums)]\n        \n        ans = 0\n        curr = 0\n        \n        for i in range(len(diff)):\n            if i == 0 or (diff[i - 1] > 0 and diff[i] > 0) or (diff[i - 1] < 0 and diff[i] < 0):\n                add = abs(diff[i]) - abs(curr)\n                if add > 0:\n                    ans += add\n            else:\n                ans += abs(diff[i])\n            curr = diff[i]\n        \n        return ans",
    "submit_ts": "1721533542",
    "subm_id": "1328011314"
}