{
    "username": "A Kiong",
    "submission": "class Solution(object):\n    def maximumValueSum(self, board):\n        \"\"\"\n        :type board: List[List[int]]\n        :rtype: int\n        \"\"\"\n        row = set()\n        col = set()\n        ans = float(\"-inf\")\n        curr = []\n        for i in range(len(board)):\n            for j in range(len(board[0])):\n                if len(curr) == 20:\n                    heapq.heappushpop(curr, (board[i][j], i, j))\n                else:\n                    heapq.heappush(curr, (board[i][j], i, j))\n        upper = min(20, len(curr))\n        for i in range(upper):\n            for j in range(i, upper):\n                for k in range(j,upper):\n                    one = curr[i]\n                    two = curr[j]\n                    three = curr[k]\n                    col = one[1] != two[1] and two[1] != three[1] and one[1] != three[1]\n                    row = one[2] != two[2] and one[2] != three[2] and two[2] != three[2]\n                    if col and row:\n                        summ = one[0] + two[0] + three[0]\n                        ans = max(ans, summ)     \n        return ans\n\n        \n        ",
    "submit_ts": "1723908118",
    "subm_id": "1359138925"
}