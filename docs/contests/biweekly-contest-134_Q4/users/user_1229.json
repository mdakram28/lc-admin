{
    "username": "dongerkai",
    "submission": "class Solution {\n    public long countSubarrays(int[] nums, int k) {\n        Map<Integer, Integer> map = new HashMap<>();\n        long count = 0;\n        for (int num : nums) {\n            Map<Integer, Integer> mapNew = new HashMap<>();\n            if (k == num) {\n                count++;\n            }\n            mapNew.merge(num, 1, Integer::sum);\n            for (Map.Entry<Integer, Integer> entry : map.entrySet()) {\n                int key = entry.getKey();\n                int val = key & num;\n                if (val == k) {\n                    count += entry.getValue();\n                }\n                mapNew.merge(val, entry.getValue(), Integer::sum);\n            }\n            map.clear();\n            map.putAll(mapNew);\n        }\n        return count;\n    }\n}",
    "submit_ts": "1720279929",
    "subm_id": "544569619"
}