{
    "username": "ucasyfp",
    "submission": "class Solution:\n    def maxOperations(self, s: str) -> int:\n        a=s.split(\"0\")\n        if s[-1]==\"1\":\n            a.pop()\n        a = [c for c in a if c != \"\"]\n        a = [len(c) for c in a]\n        return sum(accumulate(a))",
    "submit_ts": "1721530073",
    "subm_id": "548375710"
}