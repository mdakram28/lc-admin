{
    "username": "green_pig",
    "submission": "class Solution:\n    def maxTotalReward(self, rewardValues: List[int]) -> int:\n        import numpy as np\n        v = np.array(sorted(set(rewardValues)))\n        f = np.zeros(v[-1] * 2, dtype=int)\n        for x in reversed(range(v[-1] * 2)):\n            i = bisect_right(v, x)\n            if i == len(v): f[x] = x\n            else: f[x] = f[x + v[i:]].max()\n        return f[0]",
    "submit_ts": 1717901284.0
}