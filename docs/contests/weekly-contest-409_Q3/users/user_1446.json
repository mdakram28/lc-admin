{
    "username": "Lagnika",
    "submission": "#include <vector>\n#include <set>\nusing namespace std;\n\nclass Solution {\npublic:\n    vector<int> shortestDistanceAfterQueries(int n, vector<vector<int>>& q) { // Rename the method to shortestDistanceAfterQueries\n        vector<int> d(n, 0);\n        for (int i = 1; i < n; ++i) {\n            d[i] = d[i - 1] + 1;\n        }\n\n        set<int> p;\n        for (int i = 0; i < n; ++i) {\n            p.insert(i);\n        }\n\n        vector<int> r;\n\n        for (const auto& qi : q) {\n            int l = qi[0];\n            int rt = qi[1];\n\n            auto lb = p.lower_bound(l + 1);\n            auto ub = p.lower_bound(rt);\n\n            if (lb != p.end() && ub != p.end()) {\n                p.erase(lb, ub);\n            }\n\n            r.push_back(p.size() - 1);\n        }\n\n        return r;\n    }\n};\n",
    "submit_ts": "1722743493",
    "subm_id": "1343793167"
}