{
    "username": "Andwerp",
    "submission": "typedef long long ll;\nclass Solution {\npublic:\n    long long minimumCost(int m, int n, vector<int>& horizontalCut, vector<int>& verticalCut) {\n        ll hmult = 1, vmult = 1;\n        ll ans = 0;\n        vector<pair<ll, bool>> ord(0);\n        for(int i = 0; i < horizontalCut.size(); i++){\n            ord.push_back({horizontalCut[i], true});\n        }\n        for(int i = 0; i < verticalCut.size(); i++){\n            ord.push_back({verticalCut[i], false});\n        }\n        sort(ord.begin(), ord.end());\n        reverse(ord.begin(), ord.end());\n        for(int i = 0; i < ord.size(); i++){\n            ll cost = ord[i].first;\n            bool is_h = ord[i].second;\n            if(is_h) {\n                ans += cost * hmult;\n                vmult ++;\n            }\n            else {\n                ans += cost * vmult;\n                hmult ++;\n            }\n        }\n        return ans;\n    }\n};",
    "submit_ts": "1720924806",
    "subm_id": "1320231681"
}