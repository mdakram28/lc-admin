{
    "username": "neilnaveen",
    "submission": "func minChanges(nums []int, k int) int {\n    freq := map[int]int{}\n\n    bestfreq := 0\n    onechangelist := []int{}\n    goal := 0\n    for i := 0; i < len(nums)/2; i ++ {\n        freq[abs(nums[i] - nums[len(nums) - i - 1])] ++\n        onechange := max(abs(k - nums[len(nums) - i - 1]), abs(nums[i] - k),nums[len(nums) - i - 1], nums[i] )\n        onechangelist = append(onechangelist, onechange)\n        if  freq[abs(nums[i] - nums[len(nums) - i - 1])] > bestfreq {\n            bestfreq =  freq[abs(nums[i] - nums[len(nums) - i - 1])]\n            goal = abs(nums[i] - nums[len(nums) - i - 1])\n        }\n\n    }\n    sort.Ints(onechangelist)\n    fmt.Println(bestfreq, goal)\n    res := math.MaxInt32\n    for f, amount := range freq {\n        v := sort.SearchInts(onechangelist, f)\n        cur :=  (len(onechangelist) - v) - amount + 2 * (len(nums) / 2 - (len(onechangelist) - v)) \n        res = min(res, cur)\n    }\n    return res\n    \n}\nfunc abs(i int) int {\n    if i > 0 {\n        return i\n    }\n    return -i\n}",
    "submit_ts": "1721488272",
    "subm_id": "1327417057"
}