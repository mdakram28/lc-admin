{
    "username": "wadhwaaman1013",
    "submission": "from collections import deque\nclass Solution:\n    def numberOfAlternatingGroups(self, colors: List[int], k: int) -> int:\n        start = 0\n        startval = colors[0]\n        res = 0\n        for i, val in enumerate(colors):\n            if i == 0:\n                continue\n            if (i - start) >= k:\n                res += 1\n                start += 1\n\n            diff = i - start\n            if abs(colors[start] - val) == diff % 2:\n                continue\n            else:\n                start = i\n                startval = val\n\n\n        for i in range(len(colors), len(colors) + k):\n            print(start)\n            j = i % len(colors)\n            if (i - start) >= k:\n                res += 1\n                start += 1\n\n            diff = i - start\n            if abs(colors[j] - colors[start % len(colors)]) == diff % 2:\n                continue\n            else:\n                start = i\n                startval = colors[j]\n\n        return res",
    "submit_ts": "1720278130",
    "subm_id": "1311695678"
}