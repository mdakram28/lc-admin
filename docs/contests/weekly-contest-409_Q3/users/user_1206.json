{
    "username": "maxuanquang",
    "submission": "class Solution:\n    def shortestDistanceAfterQueries(self, n: int, queries: List[List[int]]) -> List[int]:\n        cities = list(range(n))\n\n        answer = []\n        left = inf\n        right = -inf\n        for a, b in queries:\n\n            l = bisect_right(cities, a)\n            r = bisect_left(cities, b) - 1\n            if l <= r:\n                for i in range(r, l - 1, -1):\n                    cities.pop(i)\n            \n            answer.append(len(cities) - 1)\n\n        return answer",
    "submit_ts": "1722743413",
    "subm_id": "1343791390"
}