{
    "username": "Tushar_Coder",
    "submission": "class Solution {\npublic:\n    long long minimumOperations(vector<int>& nums, vector<int>& target) {\n        for(int i = 0; i<nums.size(); i++) \n            nums[i] -= target[i];\n\n        // for(int i : nums)\n        //     cout<<i<<' ';\n        // cout<<endl;\n        //Convert all elements of nums to zero.\n\n        long long ans = 0;\n        ans += abs(nums[0]);\n        for(int i= 1; i<nums.size(); i++) {\n            if(nums[i] < 0 && nums[i-1] < 0)\n                ans += max(nums[i-1] - nums[i], 0);\n            else if(nums[i] >= 0 && nums[i-1] >= 0)\n                ans += max(nums[i] - nums[i-1], 0);\n            else\n                ans += abs(nums[i]);\n        }\n\n        return ans;\n\n    }\n};",
    "submit_ts": "1721532665",
    "subm_id": "1327991189"
}