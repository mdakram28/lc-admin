{
    "username": "wanderlust312",
    "submission": "class Solution:\n    def minimumSum(self, grid: List[List[int]]) -> int:\n        def helper1(x1,y1,x2,y2):    \n            minx,miny=1000,1000\n            maxx,maxy=-1,-1\n            for i in range(x1,x2+1):\n                for j in range(y1,y2+1):\n                        if grid[i][j]==1:\n                            minx=min(minx,i)\n                            miny=min(miny,j)\n                            maxx=max(maxx,i)\n                            maxy=max(maxy,j)\n            return (maxx-minx+1)*(maxy-miny+1)\n        \n        \n        \n        def helper2y(x1,y1,x2,y2):\n            res=sys.maxsize\n            for b in range(y1,y2):\n                res=min(res,helper1(x1,y1,x2,b)+helper1(x1,b+1,x2,y2))\n            return res\n        \n        def helper2x(x1,y1,x2,y2):\n            res=sys.maxsize\n            for a in range(x1,x2):\n                res=min(res,helper1(x1,y1,a,y2)+helper1(a+1,y1,x2,y2))\n            return res\n        \n            \n            \n        minX,minY=1000,1000\n        maxX,maxY=-1,-1\n        for i in range(len(grid)):\n            for j in range(len(grid[0])):\n                if grid[i][j]==1:\n                    minX=min(minX,i)\n                    maxX=max(maxX,i)\n                    minY=min(minY,j)\n                    maxY=max(maxY,j)\n        \n        \n        ans=sys.maxsize\n        for i in range(minX,maxX):\n            ans=min(ans,helper2y(minX,minY,i,maxY)+helper1(i+1,minY,maxX,maxY))\n            ans=min(ans,helper1(minX,minY,i,maxY)+helper2y(i+1,minY,maxX,maxY))\n        \n        for j in range(minY,maxY):\n            ans=min(ans,helper2x(minX,minY,maxX,j)+helper1(minX,j+1,maxX,maxY))\n            ans=min(ans,helper1(minX,minY,maxX,j)+helper2x(minX,j+1,maxX,maxY))\n        \n        \n        \n        for i1 in range(minX,maxX-1):\n            for i2 in range(i1+1,maxX):\n                ans=min(ans,helper1(minX,minY,i1,maxY)+helper1(i1+1,minY,i2,maxY)+helper1(i2+1,minY,maxX,maxY))        \n        \n        for j1 in range(minY,maxY-1):\n            for j2 in range(j1+1,maxY):\n                ans=min(ans,helper1(minX,minY,maxX,j1)+helper1(minX,j1+1,maxX,j2)+helper1(minX,j2+1,maxX,maxY))\n        \n        return ans\n            \n        \n                    ",
    "submit_ts": 1719114336.0
}