{
    "username": "kishanvaghamashi2001",
    "submission": "class Solution:\n    def countSubarrays(self, nums: List[int], k: int) -> int:\n        n = len(nums)\n        count = 0\n        \n        prev_results = {}\n\n        for i in range(n):\n            curr_results = {}\n\n            if nums[i] == k:\n                count += 1\n            curr_results[nums[i]] = 1\n\n            for val, freq in prev_results.items():\n                new_and_result = val & nums[i]\n                if new_and_result == k:\n                    count += freq\n                curr_results[new_and_result] = curr_results.get(new_and_result, 0) + freq\n            \n            prev_results = curr_results\n        \n        return count",
    "submit_ts": "1720278482",
    "subm_id": "1311711097"
}