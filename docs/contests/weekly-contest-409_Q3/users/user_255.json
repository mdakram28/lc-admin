{
    "username": "fuwutu",
    "submission": "from sortedcontainers import SortedList\n\nclass Solution:\n    def shortestDistanceAfterQueries(self, n: int, queries: List[List[int]]) -> List[int]:\n        res = []\n        sl = SortedList([i for i in range(n)])\n        for u, v in queries:\n            i = sl.bisect_left(u)\n            if sl[i] == u:\n                j = sl.bisect_left(v)\n                if sl[j] == v:\n                    if i + 1 < j:\n                        del sl[i+1:j]\n            res.append(len(sl) - 1)\n        return res",
    "submit_ts": "1722740393",
    "subm_id": "552331218"
}