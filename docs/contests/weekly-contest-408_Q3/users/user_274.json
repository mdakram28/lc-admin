{
    "username": "Preditor",
    "submission": "class Solution {\n    public int numberOfSubstrings(String s) {\n        int output = 0;\n        int prefix[] = new int[s.length()];\n        for(int i=0;i<s.length();i++){\n            if(i == 0) prefix[i] = (s.charAt(i) == '1')? 1:0;\n            else prefix[i] = prefix[i-1] + ((s.charAt(i) == '1')? 1:0);\n        }\n        //System.out.println(Arrays.toString(prefix));\n        for(int i=0;i<s.length();i++){\n            int count = 0,one = 0, jk = 0;boolean check = false;\n            for(int j=i;j<s.length();j++){\n                one = prefix[j]-((i == 0)?0:prefix[i-1]);\n                count = j-i+1-one;\n                if(count*count > one) j+=((count*count)-one-1);\n                if(count*count <= one) {\n                    int kl = (int)Math.sqrt(one);\n                    output++;\n                    if(kl>count){\n                        //System.out.println(\"aaya\");\n                        output += (j+(kl-count))>=s.length()?(s.length()-j-1):(kl-count);\n                        j = j+(kl-count);\n                    }\n                    // output += (one == (count*count)?1:((j+one-(count*count))>=s.length())?(s.length()-j):(one-(count*count)));\n                    // System.out.println(i+\"  \"+j+\"   \"+one+\"   \"+count+\"   \"+output);\n                    // j = j+(one == (count*count)?1:((j+one-(count*count))>=s.length())?(s.length()-j):(one-(count*count)))-1;\n                    //if(op == 0) break;\n                    // if((j+op)<s.length()){\n                    //     one = prefix[j+op]-((i == 0)?0:prefix[i-1]);\n                    //     count = j+op-i+1-one;\n                    // }\n                    // if(count*count <= one) output += (op)-1;\n                    // System.out.println(i+\"   \"+j+\"  \"+count+\"   \"+one+\"   \"+output+\"   \"+((count*count)-one-1)+\"   \"+op);\n                    // j = j+op;\n                }\n                //System.out.println(i+\"   \"+j+\"  \"+count+\"   \"+one+\"   \"+output+\"   \"+((count*count)-one-1));\n            }\n        }\n        return output;\n    }\n}",
    "submit_ts": "1722138480",
    "subm_id": "1335791864"
}