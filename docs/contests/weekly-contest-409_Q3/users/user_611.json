{
    "username": "braindroid",
    "submission": "class Solution {\npublic:\n    vector<int> shortestDistanceAfterQueries(int n, vector<vector<int>>& queries) {\n        set<int>s;\n        for(int i = 0 ; i < n ; i++) {\n            s.insert(i);\n        }\n        vector<int>d(n,0);\n        for(int i = 0 ; i < n ; i++) {\n            d[i] = i;\n        }\n        vector<int>ans;\n        for(auto &x : queries) {\n            int l = x[0];\n            int r = x[1];\n            int D = 1;\n            while(!s.empty() && s.upper_bound(l) != s.end() && *s.upper_bound(l) < r) {\n                int val = *s.upper_bound(l);\n                s.erase(val);\n            }\n            int res = (int)s.size() - 1;\n            ans.push_back(res);\n        }\n        return ans;\n    }\n};",
    "submit_ts": "1722742445",
    "subm_id": "1343769121"
}