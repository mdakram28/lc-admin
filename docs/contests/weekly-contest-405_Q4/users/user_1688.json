{
    "username": "tea9296",
    "submission": "class TrieNode {\npublic:\n    unordered_map<char, TrieNode*> children;\n    vector<pair<int, int>> word_ends;\n};\n\nclass Trie {\npublic:\n    TrieNode* root;\n\n    Trie() {\n        root = new TrieNode();\n    }\n\n    void insert(string word, int index, int cost) {\n        TrieNode* node = root;\n        for (char c : word) {\n            if (!node->children.count(c)) {\n                node->children[c] = new TrieNode();\n            }\n            node = node->children[c];\n        }\n        node->word_ends.push_back({index, cost});\n    }\n};\n\nclass Solution {\npublic:\n    int  minimumCost(string target, vector<string>& words, vector<int>& costs) {\n        int n = target.size();\n        vector<int> dy(n + 1, INT_MAX);\n        dy[0] = 0;\n\n        Trie trii;\n        for (int i = 0; i < words.size(); ++i) {\n            trii.insert(words[i], i, costs[i]);\n        }\n\n        for (int i = 0; i < n; ++i) {\n            if (dy[i] == INT_MAX) {\n                continue;\n            }\n\n            TrieNode* node = trii.root;\n            for (int j = i; j < n; ++j) {\n                if (!node->children.count(target[j])) {\n                    break;\n                }\n                node = node->children[target[j]];\n                for (auto& p : node->word_ends) {\n                    int word_len = words[p.first].size();\n                    if (i + word_len <= n) {\n                        dy[i + word_len] = min(dy[i + word_len], dy[i] + p.second);\n                    }\n                }\n            }\n        }\n\n        return dy[n] == INT_MAX? -1 : dy[n];\n    }\n};",
    "submit_ts": "1720324490",
    "subm_id": "1312400793"
}