{
    "username": "Roroyal",
    "submission": "class Solution {\npublic:\n    vector<bool> getResults(vector<vector<int>>& queries) {\n        vector<bool> ans;\n        int q = queries.size();\n        \n        vector<int> bar;\n        bar.push_back(0);\n        bar.push_back(50001);\n        for(int i=0; i<q; i++){\n            if(queries[i][0] == 1){\n                bar.push_back(queries[i][1]);\n            }\n        }\n        \n        sort(bar.begin(), bar.end());\n        \n        map<int, pair<int,int>> mp;\n        mp[bar[0]] = {bar[0], bar[1]};\n        for(int i=1; i<bar.size()-1; i++){\n            mp[bar[i]] = {bar[i-1], bar[i+1]};\n        }\n        mp[bar[bar.size()-1]] = {bar[bar.size()-2], 0};\n        \n        set<pair<int,int>> s;\n        set<pair<int,int>>::iterator it;\n\n        \n        for(int i=0; i<bar.size()-1; i++){\n            s.insert({bar[i+1]-bar[i], bar[i]});\n        }\n        s.insert({0,bar[bar.size()-1]});\n        \n        // for(it = s.begin(); it!= s.end(); it++){\n        //     cout << it->first << \" \" << it->second << '\\n';\n        // }\n        \n        for(int i=q-1; i>=0; i--){\n            if(queries[i][0] == 1){\n                int x = queries[i][1];\n                int x_f = mp[x].first;\n                int x_l = mp[x].second;\n                \n                s.erase({x-x_f, x_f});\n                s.erase({x_l-x, x});\n                mp[x_f].second = x_l;\n                mp[x_l].first = x_f;\n                s.insert({x_l-x_f, x_f});\n            }\n            else{\n                int x = queries[i][1];\n                int y = queries[i][2];\n                if(y>x){\n                    ans.push_back(false);\n                    continue;\n                }\n                \n                it = s.lower_bound({y,0});\n                bool can = false;\n                for (it; it!=s.end(); it++){\n                    if(y + it->second <= x){\n                        \n                        can = true;\n                        break;\n                    }\n                }\n                \n                ans.push_back(can);\n                \n                \n            }\n        }\n        \n        \n        reverse(ans.begin(), ans.end());\n        \n        \n        return ans;\n    }\n};",
    "submit_ts": 1716650953.0
}