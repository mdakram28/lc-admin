{
    "username": "megurine",
    "submission": "class Solution:\n    def minimumDifference(self, nums: List[int], k: int) -> int:\n        p = set([nums[0]])\n        ans = abs(k - nums[0])\n        for x in nums:\n            p = set([x] + [x & y for y in p])\n            for y in p:\n                cur = abs(k - y)\n                if cur < ans:\n                    ans = cur\n        return ans",
    "submit_ts": "1717296123",
    "subm_id": "1274691215"
}