{
    "username": "shin_718",
    "submission": "#include <vector>\n#include <map>\n#include <cmath>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int k;\n    \n    int helper(vector<int>& nums, int a) {\n        int cnt = 0;\n        for (int i = 0; i < nums.size() / 2; i++) {\n            if(abs(nums[i] - nums[nums.size() - 1 - i])== a)continue;\n            if (abs(nums[i] - nums[nums.size() - 1 - i]) > a) {\n                cnt++;\n            } else if (abs(nums[i] - nums[nums.size() - 1 - i]) < a) {\n                int maxi = max(nums[i], nums[nums.size() - 1 - i]);\n                int mini = min(nums[i], nums[nums.size() - 1 - i]);\n                if (max(k,maxi) - mini < a && maxi < a) {\n                    cnt += 2;\n                } else {\n                    cnt++;\n                }\n            }\n        }\n        return cnt;\n    }\n    \n    int minChanges(vector<int>& nums, int k) {\n        map<int, int> mp;\n        this->k = k;\n        int mx = -1;\n        for (int i = 0; i < nums.size()/2; i++) {\n            mp[abs(nums[i] - nums[nums.size() - 1 - i])]++;\n        }\n        map<int,int>m;\n        set<int>st,st1;\n        for(auto it:mp){\n           if(st1.find(it.second)==st1.end()){\n               st.insert(it.first);\n               st1.insert(it.second);\n        }\n        }\n//         int maxi = 0;\n//         int a = -1;\n//         for (auto it : mp) {\n//             if (it.second > maxi) {\n//                 maxi = it.second;\n//                 a = it.first;\n//             }\n//         }\n        \n//         int cnt = 0;\n        // int ans = nums.size();\n        // int l = 0, r = k;\n        // while (l <= r) {\n        //     int mid = l + (r - l) / 2;\n        //     int j = helper(nums, mid);\n        //     if (j <= ans) {\n        //         ans = j;\n        //         r = mid - 1;\n        //     } else {\n        //         l = mid + 1;\n        //     }\n        // }\n        int ans=1e5;\n            \n            for(auto it:st){\n                ans=min(ans,helper(nums,it));\n            }\n        return ans;\n    }\n};\n\n/*\n[9,2,7,7,8,9,1,5,1,9,4,9,4,7]\n9\n*/",
    "submit_ts": "1721489278",
    "subm_id": "1327448452"
}