{
    "username": "xinyueya",
    "submission": "class Solution:\n    def minimumOperations(self, nums: List[int], target: List[int]) -> int:\n        diff = [0] + [target[i] - nums[i] for i in range(len(nums))] + [0]\n        dd = [abs(diff[i] - diff[i - 1]) for i in range(1, len(diff))]\n\n        return sum(dd) // 2\n\n",
    "submit_ts": "1721533276",
    "subm_id": "1328005325"
}