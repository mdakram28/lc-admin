{
    "username": "atharvaparab9160",
    "submission": "from typing import List\nfrom bisect import bisect_left\n\nclass Solution:\n    def minChanges(self, nums: List[int], k: int) -> int:\n        from collections import defaultdict\n        def maxi(l):\n            return max(l)\n        def mini(l):\n            return mini(l)\n        def con(a,b):\n            return a<b\n        def d(iter2,iter1):\n            return iter2//iter1\n        def s(iter2,iter1):\n            return iter2-iter1\n        def dict_d(): \n            return 9\n        ddiict = defaultdict(list)\n        vertex = []\n        len_nums = len(nums)\n        \n        for iterate in range(d(len_nums , 2)):\n            iter1,iter2 = nums[len_nums - iterate -1],nums[iterate]\n             \n            \n            change = abs(s(iter2 , iter1))\n            to_put = maxi([iter2, iter1, s(k , iter2), s(k , iter1)])\n            ddiict[change].append(to_put)\n            vertex.append(to_put)\n        \n        \n        vertex=sorted(vertex)\n        def mul(a,b):\n            return a*b\n        def add(a,b):\n            return a+b\n        uttar = len_nums\n        for change, item_2 in ddiict.items():\n            to_mark_bisect = bisect_left(vertex, change)\n            l=(s(d(len_nums , 2) , to_mark_bisect))\n            to_mark_bisect = add(mul(to_mark_bisect , 2) , l)\n            \n            for to_put in item_2:\n                if con(to_put , change) == True :\n                    to_mark_bisect =s(to_mark_bisect,2)\n                else:\n                    to_mark_bisect = s(to_mark_bisect,1)\n            \n            uttar = min([uttar, to_mark_bisect])\n        \n        return uttar\n",
    "submit_ts": "1721490499",
    "subm_id": "1327484348"
}