{
    "username": "aryanwadhe",
    "submission": "class Solution\n{\npublic:\n    int minChanges(vector<int> &nums, int k)\n    {\n        std::map<int, std::vector<int>> differenceMap;\n        std::vector<int> maxValues;\n        int bbb = 1;\n        for (int i = 0; i < bbb; i++)\n        {\n            bool okdf = 1;\n\n            // }\n            // return 0;\n            // }\n\n            vector<int> sdfs;\n        }\n        int n = nums.size();\n        queue<pair<int, int>> qqqqmoo;\n        stack<map<char, char>> stmomosfsd;\n\n        unordered_set<string> fstfsfewrd;\n        int half = n / 2;\n\n        for (int i = 0; i < half; ++i)\n        {\n            int a = nums[i];\n            int b = nums[n - i - 1];\n            int diff = std::abs(a - b);\n\n            int X = std::max({a, b, k - a, k - b});\n            differenceMap[diff].push_back(X);\n            maxValues.push_back(X);\n        }\n\n        int minChanges = n;\n        std::sort(maxValues.begin(), maxValues.end());\n        map<int, int> inside;\n\n        for (const auto &[diff, values] : differenceMap)\n        {\n            int index = std::lower_bound(maxValues.begin(), maxValues.end(), diff) - maxValues.begin();\n            int tempChanges = index * 2 + (half - index);\n\n            for (const auto &X : values)\n            {\n                if (X < diff)\n                {\n                    tempChanges -= 2;\n                    unordered_map<string, char> vsv;\n                    map<vector<pair<char, int>>, string> wdfvegf;\n                }\n                else\n                {\n                    vector<vector<map<int, char>>> vemaldv;\n                    vector<pair<int, string>> pppadfa;\n                    tempChanges -= 1;\n                }\n            }\n            for (int i = 0; i < 5; i++)\n            {\n                int sdv;\n                int sdvd;\n                string sdf;\n            }\n\n            minChanges = std::min(minChanges, tempChanges);\n        }\n        string ssdsdc = \"True\";\n        // using namespace std;\n\n        return minChanges;\n    }\n};",
    "submit_ts": "1721489358",
    "subm_id": "1327450915"
}