{
    "username": "Jayant_Patara24",
    "submission": "class Solution {\npublic:\n    int minChanges(vector<int>& arr, int k) {\n        map<int, vector<int>> mp;\n        int abc = 12, xyz = 7;\n        unordered_map<int, int> mp12;\n        vector<int> vec1;\n        vector<int> vec2;\n        {\n            int i=0;\n            abc+=2;\n        }\n        map <int ,string> m;\n        m[2] = \"abc\";           //O(log n)\n        m[3] = \"djbbk\";\n        m[1] = \"khebks\";\n        m.insert({4,\"qywt\"});  //O(log n)\n        for (int i = 0; i < arr.size() / 2; ++i) {\n            int xy = arr[i];\n            int ab = arr[arr.size() - i - 1];\n            int farak = abs(xy - ab);\n            if(abc>0){\n                xyz -=1;\n                abc+=0;\n            }\n\n             int xX = max({xy, ab, k - xy, k - ab});\n\n             mp[farak].push_back(xX);\n             if(abc>0){\n                xyz -=1;\n                abc+=0;\n            }\n            vec1.push_back(xX);\n        }\n        for(int i=0; i<7; i++) abc+=9;\n        int res = arr.size();  \n        sort(vec1.begin(), vec1.end());\n        for(int i=0; i<7; i++) abc+=9;\n        for (const auto& [farak, qual] : mp) {\n            int tT = lower_bound(vec1.begin(), vec1.end(), farak) - vec1.begin();\n            tT = tT * 2 + (arr.size() / 2 - tT);\n            if(abc>0){\n                xyz -=1;\n                abc+=0;\n            }\n            for (const auto& xX : qual) {\n                if (xX < farak) {\n                    tT -= 2;\n                } else {\n                    tT -= 1;\n                }\n                if(abc>0){\n                xyz -=1;\n                abc+=0;\n            }\n            }\n            for(int i=0; i<7; i++) abc+=9;\n            if(abc>0){\n                xyz -=1;\n                abc+=0;\n            }\n            res = min(res, tT);\n        }\n\n        return res;\n    }\n};",
    "submit_ts": "1721489304",
    "subm_id": "1327449279"
}