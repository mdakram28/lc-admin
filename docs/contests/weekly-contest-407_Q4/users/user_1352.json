{
    "username": "yashraj_prasad",
    "submission": "class Solution:\n    def minimumOperations(self, nums: List[int], target: List[int]) -> int:\n        diffs = [target[i] - nums[i] for i in range(len(nums))]\n        c, prev = 0,0\n        val = []\n        \n        for i in range(len(diffs)):\n            if i == 0 or (diffs[i - 1] > 0 and diffs[i] > 0) or (diffs[i - 1] < 0 and diffs[i] < 0):\n                req = abs(diffs[i]) - abs(prev)\n                if req > 0:\n                    c += req\n                prev = diffs[i]\n            else:\n                c += abs(diffs[i])\n                prev = diffs[i]\n        \n        return c\n        ",
    "submit_ts": "1721533055",
    "subm_id": "1328000244"
}