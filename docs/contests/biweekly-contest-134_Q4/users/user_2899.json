{
    "username": "Harshit-code-tech",
    "submission": "class Solution:\n    def countSubarrays(self, nums: List[int], k: int) -> int:\n        n = len(nums)\n        total = 0\n        previous_results = defaultdict(int)       \n        for num in nums:\n            current_results = defaultdict(int)           \n            if num == k:\n                total += 1            \n            current_results[num] += 1            \n            for value, frequency in previous_results.items():\n                new_and_result = value & num                \n                if new_and_result == k:\n                    total += frequency                \n                current_results[new_and_result] += frequency            \n            previous_results = current_results       \n        return total",
    "submit_ts": "1720278847",
    "subm_id": "1311726789"
}