{
    "username": "HyperionLegion",
    "submission": "from sortedcontainers import SortedList, SortedSet, SortedDict \n\nclass Solution:\n    def shortestDistanceAfterQueries(self, n: int, queries: List[List[int]]) -> List[int]:\n        cities = SortedList([i for i in range(n)])\n        ans = []\n        for query in queries:\n            for i in range(bisect.bisect_right(cities, query[1]-1)-1, bisect.bisect_left(cities, query[0]+1)-1, -1):\n                #if cities[i]!=query[0] and cities[i]!=query[1]:\n                    cities.discard(cities[i])\n            ans.append(len(cities)-1)\n        return ans",
    "submit_ts": "1722743503",
    "subm_id": "1343793383"
}