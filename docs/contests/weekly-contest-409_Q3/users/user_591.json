{
    "username": "roxyfan",
    "submission": "from sortedcontainers import SortedSet\n\nclass Solution:\n    def shortestDistanceAfterQueries(self, n: int, queries: List[List[int]]) -> List[int]:\n        nodes = SortedSet(range(n))\n        ans = []\n        for a, b in queries:\n            i = nodes.bisect_left(a + 1)\n            while i < len(nodes) and nodes[i] < b:\n                nodes.pop(i)\n            ans.append(len(nodes) - 1)\n\n        return ans",
    "submit_ts": "1722740588",
    "subm_id": "1343723640"
}