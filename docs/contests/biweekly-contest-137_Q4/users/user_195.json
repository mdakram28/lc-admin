{
    "username": "ZCode",
    "submission": "class Solution:\n    def maximumValueSum(self, board: List[List[int]]) -> int:\n        n, m = len(board), len(board[0])\n        mx = [[] * 3 for _ in range(n)]\n        for i, r in enumerate(board):\n            cur = sorted([(-x, idx) for idx, x in enumerate(r)])\n            mx[i] = cur[:3]\n        mx.sort()\n        # mx2 = [mx[0]]\n        # for i in range(1, n):\n        #     if mx[i] != mx2[-1]:\n        #         mx2.append(mx[i])\n        # mx = mx2\n        \n        def cal(a, b, c):\n            ans = -inf\n            for i in range(3):\n                for j in range(3):\n                    for k in range(3):\n                        if a[i][1] == b[j][1] or a[i][1] == c[k][1] or b[j][1] == c[k][1]:\n                            continue\n                        ans = max(ans, -(a[i][0]+b[j][0]+c[k][0]))\n            return ans\n\n        print(mx)\n        ans = -inf\n        for i in range(min(50, n)):\n            for j in range(i+1, min(i+50, n)):\n                for k in range(j+1, min(j+50, n)):\n                    ans = max(ans, cal(mx[i], mx[j], mx[k]))\n        return ans\n",
    "submit_ts": "1723907109",
    "subm_id": "556323628"
}