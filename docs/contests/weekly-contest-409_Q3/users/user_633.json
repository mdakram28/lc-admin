{
    "username": "Vintersosa",
    "submission": "from sortedcontainers import SortedSet\nclass Solution:\n    def shortestDistanceAfterQueries(self, n: int, queries: List[List[int]]) -> List[int]:\n\n        sorted_set = SortedSet() \n        for i in range(n):\n            sorted_set.add(i)\n        result = []\n        for x,y in queries:\n            ini = sorted_set.bisect_right(x)\n            while(sorted_set[ini]<y):\n                sorted_set.pop(ini)\n            result.append(len(sorted_set)-1)\n        return result\n            \n        \n\n\n        \n\n                \n\n",
    "submit_ts": "1722742217",
    "subm_id": "1343763781"
}