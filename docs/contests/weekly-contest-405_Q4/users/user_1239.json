{
    "username": "fishlov",
    "submission": "class Solution:\n    def minimumCost(self, target: str, words: List[str], costs: List[int]) -> int:\n        dp = [float(\"inf\") for _ in range(len(target) + 1)]\n        dp[0] = 0\n        \n        costs_adj = {}\n        \n        for i in range(len(words)):\n            costs_adj[words[i]] = min(costs_adj.get(words[i], float(\"inf\")), costs[i])\n        \n        for i in range(1, len(dp)):\n            for word, cost in costs_adj.items():\n                if word == target[i - len(word):i]:\n                    dp[i] = min(dp[i], dp[i - len(word)] + cost)\n        \n        return dp[-1] if dp[-1] != float(\"inf\") else -1",
    "submit_ts": "1720322854",
    "subm_id": "1312360830"
}