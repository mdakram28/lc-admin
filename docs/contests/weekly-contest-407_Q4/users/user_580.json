{
    "username": "indian_czar",
    "submission": "class Solution {\npublic:\n    long long max(long long a,long long b){\n        if(a>b) return a;\n        return b;\n    }\n    long long min(long long a,long long b){\n        if(a<b) return a;\n        return b;\n    }\n    long long minimumOperations(vector<int>& nums, vector<int>& target) {\n        vector<long long > diff;\n        for(int i=0;i<nums.size();i++){\n            diff.push_back(nums[i]-target[i]);\n        }\n        long long ans=0;\n        long long temp=0;\n        for(int i=0;i<diff.size();i++){\n            if(diff[i]==0){\n                temp=0;\n            }\n            else if(diff[i]==temp){\n\n            }\n            else if(diff[i]>0){\n                temp=max(temp,0);\n                ans+=max(0,diff[i]-temp);\n                // temp+=max(0,diff[i]-temp);\n                temp=diff[i];\n            }\n            else{\n                temp=min(temp,0);\n                ans+=max(0,temp-diff[i]);\n                // temp-=abs(min(0,diff[i]-temp)-0);\n                temp=diff[i];\n            }\n        }\n        return ans;\n    }\n    \n};",
    "submit_ts": "1721531488",
    "subm_id": "1327962194"
}