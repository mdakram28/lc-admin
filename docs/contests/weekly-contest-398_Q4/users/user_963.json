{
    "username": "i_m__prince",
    "submission": "class Solution:\n    def waysToReachStair(self, k: int) -> int:\n        total = 0\n        for i in range(32):\n            ways = i + 1\n            exponent = 2**i - k\n            if exponent >= 0:\n                total += math.comb(ways, exponent)\n        return total",
    "submit_ts": 1716090255.0
}