{
    "username": "sumon_1212",
    "submission": "class Solution {\npublic:\n    int ans=1000;\n    void ok(int a1,int b1,int c1,int d1,int a2,int b2,int c2,int d2,int a3,int b3,int c3,int d3)\n    {\n        if(a1<=b1&&c1<=d1&&a2<=b2&&c2<=d2&&a3<=b3&&c3<=d3)\n        {\n            ans=min(ans,(b1-a1+1)*(d1-c1+1)+(b2-a2+1)*(d2-c2+1)+(b3-a3+1)*(d3-c3+1));\n            //cout<<a1<<\" \"<<b1<<\" \"<<c1<<\" \"<<d1<<endl;\n        }\n    }\n    int minimumSum(vector<vector<int>>& grid) {\n        vector<pair<int,int>>v;\n        for(int i=0;i<grid.size();i++)\n        {\n            for(int j=0;j<grid[0].size();j++)\n            {\n                if(grid[i][j])\n                {\n                    //cout<<i<<j<<endl;\n                    v.push_back({i,j});\n                }\n            }\n        }\n        for(int i=0;i<grid.size();i++)\n        {\n            vector<pair<int,int> >v1;\n            int r1min=100,r1max=0,c1min=100,c1max=0;\n            for(auto a:v)\n            {\n                if(a.first<=i)\n                {\n                    v1.push_back(a);\n                }\n                else\n                {\n                    r1min=min(r1min,a.first);\n                    r1max=max(r1max,a.first);\n                    c1min=min(c1min,a.second);\n                    c1max=max(c1max,a.second);\n                }\n            }\n            if(v1.size()>=2)\n            {\n                sort(v1.begin(),v1.end());\n                for(int j=0;j<v1.size();j++)\n                {\n                    int r2min=100,r2max=0,c2min=100,c2max=0,r3min=100,r3max=0,c3min=100,c3max=0;\n                    for(auto a:v1)\n                    {\n                        if(a.second<=v1[j].second)\n                        {\n                            r2min=min(r2min,a.first);\n                            r2max=max(r2max,a.first);\n                            c2min=min(c2min,a.second);\n                            c2max=max(c2max,a.second);\n                        }\n                        else\n                        {\n                            r3min=min(r3min,a.first);\n                            r3max=max(r3max,a.first);\n                            c3min=min(c3min,a.second);\n                            c3max=max(c3max,a.second);\n                        }\n                    }\n                    ok(r1min,r1max,c1min,c1max,r2min,r2max,c2min,c2max,r3min,r3max,c3min,c3max);\n                }\n                for(int j=0;j<v1.size();j++)\n                {\n                    int r2min=100,r2max=0,c2min=100,c2max=0,r3min=100,r3max=0,c3min=100,c3max=0;\n                    for(auto a:v1)\n                    {\n                        if(a.first<=v1[j].first)\n                        {\n                            r2min=min(r2min,a.first);\n                            r2max=max(r2max,a.first);\n                            c2min=min(c2min,a.second);\n                            c2max=max(c2max,a.second);\n                        }\n                        else\n                        {\n                            r3min=min(r3min,a.first);\n                            r3max=max(r3max,a.first);\n                            c3min=min(c3min,a.second);\n                            c3max=max(c3max,a.second);\n                        }\n                    }\n                ok(r1min,r1max,c1min,c1max,r2min,r2max,c2min,c2max,r3min,r3max,c3min,c3max);\n            }\n            }\n            r1min=100,r1max=0,c1min=100,c1max=0;\n            v1.clear();\n            for(auto a:v)\n            {\n                if(a.first<=i)\n                {\n                    r1min=min(r1min,a.first);\n                    r1max=max(r1max,a.first);\n                    c1min=min(c1min,a.second);\n                    c1max=max(c1max,a.second);\n                }\n                else\n                {\n                    v1.push_back(a);\n                }\n            }\n            if(v1.size()>=2)\n            {\n                sort(v1.begin(),v1.end());\n                for(int j=0;j<v1.size();j++)\n                {\n                    int r2min=100,r2max=0,c2min=100,c2max=0,r3min=100,r3max=0,c3min=100,c3max=0;\n                    for(auto a:v1)\n                    {\n                        if(a.second<=v1[j].second)\n                        {\n                            r2min=min(r2min,a.first);\n                            r2max=max(r2max,a.first);\n                            c2min=min(c2min,a.second);\n                            c2max=max(c2max,a.second);\n                        }\n                        else\n                        {\n                            r3min=min(r3min,a.first);\n                            r3max=max(r3max,a.first);\n                            c3min=min(c3min,a.second);\n                            c3max=max(c3max,a.second);\n                        }\n                    }\n                    ok(r1min,r1max,c1min,c1max,r2min,r2max,c2min,c2max,r3min,r3max,c3min,c3max);\n                }\n                for(int j=0;j<v1.size();j++)\n                {\n                    int r2min=100,r2max=0,c2min=100,c2max=0,r3min=100,r3max=0,c3min=100,c3max=0;\n                    for(auto a:v1)\n                    {\n                        if(a.first<=v1[j].first)\n                        {\n                            r2min=min(r2min,a.first);\n                            r2max=max(r2max,a.first);\n                            c2min=min(c2min,a.second);\n                            c2max=max(c2max,a.second);\n                        }\n                        else\n                        {\n                            r3min=min(r3min,a.first);\n                            r3max=max(r3max,a.first);\n                            c3min=min(c3min,a.second);\n                            c3max=max(c3max,a.second);\n                        }\n                    }\n                ok(r1min,r1max,c1min,c1max,r2min,r2max,c2min,c2max,r3min,r3max,c3min,c3max);\n            }\n            }\n        }\n        for(int i=0;i<v.size();i++)\n        {\n            swap(v[i].first,v[i].second);\n        }\n        for(int i=0;i<grid[0].size();i++)\n        {\n            vector<pair<int,int> >v1;\n            int r1min=100,r1max=0,c1min=100,c1max=0;\n            for(auto a:v)\n            {\n                if(a.first<=i)\n                {\n                    v1.push_back(a);\n                }\n                else\n                {\n                    r1min=min(r1min,a.first);\n                    r1max=max(r1max,a.first);\n                    c1min=min(c1min,a.second);\n                    c1max=max(c1max,a.second);\n                }\n            }\n           if(v1.size()>=2)\n            {\n                sort(v1.begin(),v1.end());\n                for(int j=0;j<v1.size();j++)\n                {\n                    int r2min=100,r2max=0,c2min=100,c2max=0,r3min=100,r3max=0,c3min=100,c3max=0;\n                    for(auto a:v1)\n                    {\n                        if(a.second<=v1[j].second)\n                        {\n                            r2min=min(r2min,a.first);\n                            r2max=max(r2max,a.first);\n                            c2min=min(c2min,a.second);\n                            c2max=max(c2max,a.second);\n                        }\n                        else\n                        {\n                            r3min=min(r3min,a.first);\n                            r3max=max(r3max,a.first);\n                            c3min=min(c3min,a.second);\n                            c3max=max(c3max,a.second);\n                        }\n                    }\n                    ok(r1min,r1max,c1min,c1max,r2min,r2max,c2min,c2max,r3min,r3max,c3min,c3max);\n                }\n                for(int j=0;j<v1.size();j++)\n                {\n                    int r2min=100,r2max=0,c2min=100,c2max=0,r3min=100,r3max=0,c3min=100,c3max=0;\n                    for(auto a:v1)\n                    {\n                        if(a.first<=v1[j].first)\n                        {\n                            r2min=min(r2min,a.first);\n                            r2max=max(r2max,a.first);\n                            c2min=min(c2min,a.second);\n                            c2max=max(c2max,a.second);\n                        }\n                        else\n                        {\n                            r3min=min(r3min,a.first);\n                            r3max=max(r3max,a.first);\n                            c3min=min(c3min,a.second);\n                            c3max=max(c3max,a.second);\n                        }\n                    }\n                ok(r1min,r1max,c1min,c1max,r2min,r2max,c2min,c2max,r3min,r3max,c3min,c3max);\n            }\n            }\n            r1min=100,r1max=0,c1min=100,c1max=0;\n            v1.clear();\n            for(auto a:v)\n            {\n                if(a.first<=i)\n                {\n                    r1min=min(r1min,a.first);\n                    r1max=max(r1max,a.first);\n                    c1min=min(c1min,a.second);\n                    c1max=max(c1max,a.second);\n                }\n                else\n                {\n                    v1.push_back(a);\n                }\n            }\n            if(v1.size()>=2)\n            {\n                sort(v1.begin(),v1.end());\n                for(int j=0;j<v1.size();j++)\n                {\n                    int r2min=100,r2max=0,c2min=100,c2max=0,r3min=100,r3max=0,c3min=100,c3max=0;\n                    for(auto a:v1)\n                    {\n                        if(a.second<=v1[j].second)\n                        {\n                            r2min=min(r2min,a.first);\n                            r2max=max(r2max,a.first);\n                            c2min=min(c2min,a.second);\n                            c2max=max(c2max,a.second);\n                        }\n                        else\n                        {\n                            r3min=min(r3min,a.first);\n                            r3max=max(r3max,a.first);\n                            c3min=min(c3min,a.second);\n                            c3max=max(c3max,a.second);\n                        }\n                    }\n                    ok(r1min,r1max,c1min,c1max,r2min,r2max,c2min,c2max,r3min,r3max,c3min,c3max);\n                }\n                for(int j=0;j<v1.size();j++)\n                {\n                    int r2min=100,r2max=0,c2min=100,c2max=0,r3min=100,r3max=0,c3min=100,c3max=0;\n                    for(auto a:v1)\n                    {\n                        if(a.first<=v1[j].first)\n                        {\n                            r2min=min(r2min,a.first);\n                            r2max=max(r2max,a.first);\n                            c2min=min(c2min,a.second);\n                            c2max=max(c2max,a.second);\n                        }\n                        else\n                        {\n                            r3min=min(r3min,a.first);\n                            r3max=max(r3max,a.first);\n                            c3min=min(c3min,a.second);\n                            c3max=max(c3max,a.second);\n                        }\n                    }\n                ok(r1min,r1max,c1min,c1max,r2min,r2max,c2min,c2max,r3min,r3max,c3min,c3max);\n            }\n            }\n        }\n        return ans;\n    }\n};",
    "submit_ts": 1719114982.0
}