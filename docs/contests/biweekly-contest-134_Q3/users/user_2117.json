{
    "username": "HarshSantoshi",
    "submission": "class Solution {\npublic:\n    int numberOfAlternatingGroups(vector<int>& colors, int k) {\n        int ans = 0 ;\n        for(int i=0;i<k;i++){\n            colors.push_back(colors[i]);\n        }\n        unordered_map<string,int>mp;\n        vector<int>dp(colors.size() + 1 , 0);\n        for(int i=1;i<colors.size();i++){\n            dp[i]+= dp[i-1] + abs(colors[i] - colors[i-1]);\n        }\n        for(int i=k;i<colors.size();i++){\n            if(dp[i] - dp[i-k+1] == k-1){\n                ans ++ ;\n            }\n        }\n        return ans;\n    }\n};",
    "submit_ts": "1720279661",
    "subm_id": "1311760111"
}