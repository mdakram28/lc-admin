{
    "username": "horseshoecrab",
    "submission": "class Solution:\n    def waysToReachStair(self, k: int) -> int:\n        \n        @cache\n        def dp(i, jump, can_drop):\n            if i > k+1:\n                return 0\n            res = int(i==k)\n            if can_drop:\n                res += dp(i-1, jump, False)\n            res += dp(i+(1<<jump), jump+1, True)\n            return res\n            \n        \n        return dp(1, 0, True)\n    ",
    "submit_ts": 1716089681.0
}