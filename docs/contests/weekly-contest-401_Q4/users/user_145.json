{
    "username": "dinusX",
    "submission": "class Node:\n    def __init__(self, val):\n        self.val = val\n        self.next = None\n\nclass Solution:\n    def maxTotalReward(self, rew: List[int]) -> int:\n        rew.sort()\n        pos = 0\n        for i in range(1, len(rew)):\n            if rew[i] != rew[i-1]:\n                pos += 1\n            rew[pos] = rew[i]\n        while len(rew) > pos+1:\n            rew.pop()\n        if len(rew) == 1:\n            return rew[-1]\n        if rew[-2]+1 == rew[-1]:\n            return rew[-2] + rew[-1]\n        \n        arr = rew\n        @cache\n        def find(num):\n            pos = bisect.bisect_left(arr, num)\n            if arr[pos] == num:\n                return True\n            if pos <= 1:\n                return False\n            pos -= 1\n            mid = num // 2\n            while pos > 0 and arr[pos] > mid:\n                if find(num-arr[pos]):\n                    return True\n                pos -= 1\n            return False\n                \n            \n        \n        for i in range(rew[-1]-1, rew[-2]-1, -1):\n            if find(i):\n                return rew[-1]+i\n            \n        \n        return res\n                ",
    "submit_ts": "1717904756",
    "subm_id": "1282370891"
}