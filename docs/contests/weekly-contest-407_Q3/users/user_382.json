{
    "username": "han3000",
    "submission": "class Solution:\n    def maxOperations(self, s: str) -> int:\n        res = one = 0\n        for v, l in groupby(s):\n            if v == '1':\n                one += len(list(l))\n            else:\n                res += one\n        return res",
    "submit_ts": "1721529710",
    "subm_id": "548372687"
}